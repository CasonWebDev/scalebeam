# Database
# For local development with PostgreSQL:
# DATABASE_URL="postgresql://user:password@localhost:5432/scalebeam"
# DIRECT_URL="postgresql://user:password@localhost:5432/scalebeam"

# For Vercel Postgres (production):
# These will be automatically set by Vercel when you connect a Postgres database
# DATABASE_URL="postgres://..."
# DIRECT_URL="postgres://..."

# Note: After connecting Vercel Postgres in your Vercel project:
# 1. Go to your project settings on Vercel
# 2. Navigate to Storage -> Create Database -> Postgres
# 3. The DATABASE_URL and DIRECT_URL will be automatically added to your environment variables
# 4. Run: vercel env pull .env.local (to sync env vars locally)
